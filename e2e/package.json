{
  "name": "apptracker-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests for App Tracker",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:screenshots": "playwright test screenshots.spec.ts --headed",
    "test:screenshots:ci": "playwright test screenshots.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0"
  }
}
