# App Tracker Secrets
# WARNING: Replace these values in production!
# Consider using external-secrets, sealed-secrets, or vault
---
apiVersion: v1
kind: Secret
metadata:
  name: apptracker-server-secrets
  namespace: apptracker
  labels:
    app.kubernetes.io/name: apptracker
    app.kubernetes.io/component: server
type: Opaque
stringData:
  JWT_SECRET: "change-me-in-production-use-32-char-min"
  ADMIN_PASSWORD: "admin123"
